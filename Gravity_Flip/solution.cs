using System;
using System.Collections;

public class Kata
{
    public static int[] Flip(char dir, int[] arr)
    {
        int[] returnArr[arr.length + 1];
        if(char == 'R')
        {
            foreach (var number in arr)
            {
                for(int i = 0; i < arr.length; i++)
                {
                    
                }
            }
            return arr;
        }
        else 
        {
            Array.reverse(arr);
            return arr;
        }
    }
}